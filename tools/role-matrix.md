Role matrix — run `node scripts/role-matrix.mjs` to print the menu every role
resolves to, using the real nav logic. The capability strings in it are measured
from the live database; refresh them by running get_my_access() as each login.
